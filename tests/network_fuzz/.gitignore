# Timestamped run logs - noisy in git history. The handful of reference
# baseline results are force-added in the initial commit (see README.md);
# everything else generated by future runs is ignored here.
results/*.json
